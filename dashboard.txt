<!-- <x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Dashboard') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900">
                    {{ __("You're logged in!") }}
                </div>
            </div>
        </div>
    </div>
</x-app-layout> -->



reaction code

<form action="{{ route('reactions.react') }}" method="POST" class="mt-2">
    @csrf
    <input type="hidden" name="post_id" value="{{ $post->id }}">
    
    @foreach (['like' => '👍', 'love' => '❤️', 'haha' => '😂'] as $type => $emoji)
        <button type="submit" name="type" value="{{ $type }}" class="mr-2 hover:scale-110">
            {{ $emoji }}
            <span class="text-xs text-gray-600">
                {{ $post->reactions->where('type', $type)->count() }}
            </span>
        </button>


        
    @endforeach
</form>

            <!-- @if ($tasks->whereNull('archived_at')->count() == 0) -->
